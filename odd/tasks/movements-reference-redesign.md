# Movements reference redesign

## Objective
Match the supplied full-page Movimientos reference as closely as practical within the existing authenticated React dashboard, preserving live data, existing actions, and responsive/accessibility behavior. No sample records, invented metrics, or nonfunctional controls.

## Scope and constraints
- Existing source changes in `src/client/styles.css` (chart label rotation) are unrelated and must be preserved, not reverted or included in this task's edits beyond necessary localized changes.
- Visual hierarchy: Movimientos-specific heading, period/action toolbar, real metric strip, filters, selection controls, dense scrollable table with sticky header and footer, and a truthful informational banner only if supported by existing actions.
- Existing shell/navigation, synchronization, create/edit/view/delete, filtering, sorting, bulk assignment, loading/empty/error behavior remain intact.
- Do not copy fixed person/email, dates, counts, transaction rows, or faux notifications/export/search functionality from mockup.
- User requests few tests: one focused functional check and build, not a full broad test suite. TDD mode: off/unknown (no configured TDD in `.pi/settings.json`); runner: `node --test ...` for focused test, `npm run build` for compilation.
- Authored-line forecast: roughly 450–700 additions/deletions for genuine page and stylesheet integration. Delivery strategy: exception-ok for a single coherent commit on `framework_react`, explicitly selected by the user after the >400-line review warning. Commit and push are authorized; no PR or merge is authorized.

## Tasks
- [x] M1 — Integrate reference hierarchy and derived metrics into authenticated Movimientos screen, preserving functional controls and dynamic data. Route: delegated writer; authenticated page structure. Acceptance: dynamic movement heading, real metric strip and toolbar, search/kind filters and selection/table wired. Check: focused `node --test --test-name-pattern='React movements view|React movement filter|React movement sort|React movement selection' test/react-vite-foundation.test.js` passed (10/10), `git diff --check` passed; final build/readback remains under M2. Status: implementation done. Commit: included in the authorized single work-unit delivery below.
- [x] M2 — Align scoped CSS with reference (white cards, slate background, blue accents, dense scrollable table/sticky header, responsive layout), without changing Resumen or demo. Route: delegated writer (multi-file write across feature); responsive styling. Acceptance: responsive rules, real data/controls, 580px scrolling table and sticky header in source. Check: `npm run build` and `git diff --check` passed after correction; independent structural verifier confirmed empty state and filter counts. Status: implementation done; no browser visual comparison. Commit: included in the authorized single work-unit delivery below.
- [ ] M3 — Compare authenticated desktop/mobile rendering against the supplied design in a browser and adjust any visible discrepancies. Route: pending access to browser/session with representative data. Check: visual comparison not available in this session; no claim of pixel-identical rendering. Status: blocked/pending.
- [x] M4 — Remove redundant movements copy while retaining period-vs-filtered semantics and accessible status. Route: delegated writer (page/filter module and scoped CSS). Acceptance: period appears visibly once in its edit control, duplicated table footer/count and instructional text removed; period-wide KPI label, filtered count/status, unknown-amount notice, selection controls and actions retained. Check: expanded focused movement tests 11/11, `npm run build` passed (large-chunk warning), `git diff --check` clean and independent structural check. Status: implementation done. Commit: included in the authorized single work-unit delivery below.
- [x] M5 — Updated focused copy assertions, including the previously omitted plural-title filter-control test; accessible active count, idle/selected states, demo isolation, and period-wide KPI label verified. Route: delegated writer on exact test-file edit surface. Check: expanded focused movement tests 11/11; independent readback confirmed no stale expected copy; diff check clean. Status: done. Commit: included in the authorized single work-unit delivery below.
- [x] M6 — Corrected stale header-refresh assertion while retaining all three `onEditPeriod` checks; now verifies `refreshFromHeader` delegates to `financialDashboard.reload()`, the busy/disabled and accessible labels, and refresh icon/copy within the button. Route: inline one-file mechanical test edit after focused reproduction. Check: exact two financial-cycle tests 2/2 passed, `git diff --check` clean. Status: done. Commit: included in the authorized single work-unit delivery below.

## Progress
M1/M2 real-data view and responsive styles implemented. After user feedback, M4 reduced duplicate headings, period/count repetition and helper copy; M5 synchronized focused assertions (the initial singular test-name pattern had missed the plural-title filter-control test). Expanded focused movement tests 11/11, build passed with Vite's >500 kB chunk warning. M6 corrected a separate stale refresh-handler assertion discovered by focused financial-summary test; the three period-edit onClick regexes were not the failure, and two targeted tests now pass 2/2. Independent readback and `git diff --check` passed. Existing chart-label rotation CSS was modified before this feature and preserved. Native risk assessment cannot resolve the untracked task document; RDD off, independent verifier ran. No commit. Visual comparison of authenticated desktop/mobile view remains pending.

## Delivery
- One work-unit commit on `framework_react`: `feat(movements): refine authenticated activity view`. Before creating it, the source diff is 576 authored additions/deletions plus this progress document; the user explicitly accepted the review-size exception and asked to push.
- The commit can be located by `git log --format=%H -1 --grep='^feat(movements): refine authenticated activity view$' framework_react`; the exact resulting SHA is mirrored in Engram after the commit, since a Git commit cannot embed its own hash in its own tree.
- No PR or merge; visual comparison (M3) remains pending.

## Next step
Commit and push the reviewed work unit, then compare the authenticated view on desktop/mobile with representative data when available.
